# Local Terraform working directory
.terraform/

# Terraform state — keep local, never commit
*.tfstate
*.tfstate.backup
.terraform.tfstate.lock.info

# Secret variable values — never commit
terraform.tfvars

# OS
.DS_Store
